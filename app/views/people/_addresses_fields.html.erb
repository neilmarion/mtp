<div id="address_fields">
    <% if person.new_record? || person.addresses.blank?%>
      <div id="addresses[0]">
        <%= text_field_tag "person[addresses_attributes][0][address]" %>
        <%= text_field_tag "person[addresses_attributes][0][latitude]" %>
        <%= text_field_tag "person[addresses_attributes][0][longitude]" %>
      </div>
    <% else %>
      <% person.addresses.each_with_index do |address, index| %>
        <div id="addresses[<%= index %>]">
          <%= text_field_tag "person[addresses_attributes][#{index}][address]", address.address %>
          <%= text_field_tag "person[addresses_attributes][0][latitude]", address.latitude %>
        <%= text_field_tag "person[addresses_attributes][0][longitude]", address.longitude %>
        </div>
      <% end %>
    <% end %>
</div>
<%= link_to image_tag("add.png") + t('.add_an_address'), '#', id: 'add_an_address' %>
